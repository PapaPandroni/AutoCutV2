# Core video processing
moviepy==2.2.1

# Audio analysis
librosa>=0.10.1

# Computer vision and image processing
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.11.0
Pillow>=10.0.0

# System Requirements (not Python packages):
# - FFmpeg/FFprobe: Required for H.265/HEVC codec detection and transcoding
#   Install with: apt install ffmpeg (Ubuntu) or brew install ffmpeg (macOS)